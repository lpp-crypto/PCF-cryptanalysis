from setuptools import find_packages, setup

setup( 
    name = "wsPRF",
    version = "1.1",
    description = "Reference implementations for various weak shallow PRFs",
    packages = find_packages(),
)


